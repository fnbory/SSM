<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="dao.TeacherDao">
	<select id="getTeacher" resultType="Teacher" parameterType="String">
		select * from teacher where user_id=#{userId}
	</select>
	<select id="gettecs" resultType="Teacher">
		select * from teacher
	</select>
	<update id="updatetecact" parameterType="Teacher">
		update teacher set name=#{tecac.name} ,cls=#{tecac.cls} where user_id=#{tecac.userId}
	</update>
	<update id="updatetec" parameterType="Teacher">
		update teacher set name=#{tec.name} ,sex=#{tec.sex} where user_id=#{tec.userId}
	</update>
	<insert id="inserttec" parameterType="Teacher">
		insert into teacher(user_id,cls,name) values (#{teacher.userId},#{teacher.cls},#{teacher.name});
	</insert>
</mapper>